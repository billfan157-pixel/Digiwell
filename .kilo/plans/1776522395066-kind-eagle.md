# Fix TypeScript Syntax Errors in useSettings.ts

## Problem
The file `src/hooks/useSettings.ts` contains multiple TypeScript syntax errors around lines 73-75 and 85-87, including:
- Malformed localStorage.getItem calls with incorrect quotes and commas
- Duplicate property assignments
- Double commas in object literals
- Unterminated string literals

## Root Cause
The errors appear to be from manual editing where syntax was broken during copy-paste operations, resulting in:
1. Incorrect syntax in localStorage.getItem call on line 73
2. Duplicate `biometricEnabled` property assignments
3. Extra comma on line 85 in the else block

## Solution
Fix the syntax errors by:
1. Correcting the localStorage.getItem syntax on line 73
2. Removing duplicate `biometricEnabled` assignments on lines 74 and 88
3. Removing the extra comma on line 85
4. Ensuring proper object literal syntax throughout

## Steps
1. Fix line 73: Change `localStorage.getItem(,biometric_enabled')` to `localStorage.getItem('biometric_enabled')`
2. Remove duplicate `biometricEnabled` line 74
3. Remove extra comma after 'Ôn hòa' on line 85
4. Remove duplicate `biometricEnabled` line 88
5. Verify TypeScript compilation passes after fixes

## Verification
- Run TypeScript compiler to ensure no errors remain
- Test the useSettings hook functionality if possible
- Check that biometric settings load correctly from localStorage</content>
<parameter name="filePath">.kilo/plans/1776522395066-kind-eagle.md